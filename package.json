{
  "name": "pixelguessr",
  "version": "0.1.0",
  "description": "Pixelated image guessing game",
  "type": "module",
  "scripts": {
    "dev": "bun index.html",
    "build": "bun build index.html --outdir dist/ --target browser && cp -r images dist/",
    "start": "bun index.html",
    "tsc": "tsc --pretty --noEmit",
    "format": "biome format --write .",
    "biome": "biome check --error-on-warnings .",
    "knip": "knip",
    "checks": "bun run format && concurrently -g -c auto 'bun run tsc' 'bun run biome' 'bun run knip' 'bun run test'",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "test:ci": "bun test --coverage --coverage-reporter=lcov",
    "prepare": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@happy-dom/global-registrator": "^20.10.2",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "bun-types": "latest",
    "concurrently": "^10.0.3",
    "knip": "^6.16.1",
    "typescript": "^5.7.0"
  }
}
